# to change the image name to 1 to n same as the trainin datasets name for single image training
def changeImgName(images_dataset):
    for idx, image in enumerate(images_dataset):


def __main__():
    images_dataset = "../../datasets/imagenet/ILSVRC/Data/CLS-LOC/train/n02219486"
    
    changeImgName(images_dataset)